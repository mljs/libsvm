const defaultState =  {
    gamma: 1,
    cost: 1
};

export default function SVCConfigReducer(state=defaultState, action) {
    switch(action.type) {
        default:
            return state
    }
}